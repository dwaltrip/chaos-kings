# Next session prompt — mid-region lane decomposition (session 5)

We are continuing "mid-region lane decomposition" work for the next-gen solver. Prior session built the first empirical loop of the prefix/lane reframe end-to-end. Orient here:

- [Session 4 notes](4.10-8-session-4-notes.md) — start here. Covers what we built, the `tight-corner-1` signal, the scorer ablation, and pointers forward.
- [Prefix/lane reframing doc](4.10-6-prefix-sets-and-lanes-reframed.md) — the conceptual framework the work serves.

Headline from last session: the reframe has concrete evidence on its canonical hard case, but the v1 scorer's divergence + articulation terms didn't contribute under the ablation we ran — depth alone got within ±1 of the full formula. Narrow claim, not categorical — see session 4 §4.

Must-do first: a broader matrix sweep on harder boards, so we have headroom to distinguish scorer variants on more than one point. Sweep params are open and worth discussing before running. Beyond that, session 4 §7 lists brainstorm topics (prefix-set-level scoring with the current signals, why divergence/AP didn't help theoretically, new tip-scorer signals, joint prefix-set quality) that are good discussion material but not committed scoped work.

General guidance, not a strict rule: before building new helpers, it tends to pay off to poke around the existing infra first. Session 4 leaned heavily on session 3's `starting-region/` annotations, session 2's oracle + blob types, and the existing `typed-command` / `renderBoard` / BFS helpers — net new machinery was small. When genuinely new helpers do emerge, the generic parts are usually worth landing in shared locations (`utils/`, `starting-region/`, etc.) rather than buried in the first module that consumes them.

CLIs available for debugging, runnable via `tools/run-from-algos.sh`: `dump-prefix-sets`, `render-board-cli`, and the starting-region `debug-dump` (with scorer heatmap pane).

Let's discuss scope and sweep params before diving in.
