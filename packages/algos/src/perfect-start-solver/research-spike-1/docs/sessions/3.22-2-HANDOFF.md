
# HANDOFF doc from prev session

Research spike session on the perfect-start-solver. Orient from:

1. `packages/algos/src/perfect-start-solver/research-spike-1/docs/STATUS.md`
2. `packages/algos/src/perfect-start-solver/research-spike-1/docs/ROADMAP.md` (thread catalog, deep-dives — threads numbered 1-13)
3. `packages/algos/src/perfect-start-solver/research-spike-1/docs/sessions/3.22-1-LOG.md`

ROADMAP.md is complete. 13 threads sequentially numbered, board structure restructured into 3 layers. Time for actual experiments.

This session: **instrumentation pass** — thread 1 (phase timing) + thread 10 measurement (inner-loop scan waste).

Thread 1 — phase timing breakdown:
- Instrument solver to report time per phase (path gen, timing table, search) and per capture target within search.
- Key question: on tight-corner boards, how much time goes to infeasible targets (24, 23) vs the successful target (22)?
- Low effort — pure instrumentation, no algorithmic changes.

Thread 10 — inner-loop scan waste measurement:
- In the same instrumentation pass, count candidates scanned vs candidates that pass the overlap check, per search node.
- Quantifies the ceiling for watched literals — if 95%+ of scans are wasted, the case is strong.

If time remains:
- **Thread 4 (BigInt benchmark)** — benchmark BigInt vs Uint32Array at 625/900+ bit mask sizes. Self-contained, half a session.

Workflow notes:
- **Check in with me before starting implementation.** Share your plan and get alignment before editing the main solver.
- Save experiment output to `experiments/output/` from the start (these are gitignored, not committed)
- Write session LOG before updating STATUS
- Commit after completing a big block of work
- At session wrap-up, write a clean copy-pasteable next-session prompt as the last message.
