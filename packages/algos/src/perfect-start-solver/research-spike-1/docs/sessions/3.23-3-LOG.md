# Session 3.23-3 — Log

## What happened

First prefix structure experiments. Built reusable prefix analysis utilities (`prefix-utils.ts`) and ran two experiments across slow + realistic boards.

**Experiment 1: Prefix enumeration** — pool sizes, per-neighbor breakdown, fan-out distribution, tip properties. See `3.23-3-prefix-enumeration-results.md` for data and key insights.

**Experiment 2: Solution spot-check** — for each burst in the solver's solution, how fan-out narrows from depth 1 through 4. Results in the same doc.

**Discussion** — brainstormed next directions: characterizing the prefix-suffix boundary, prefix set enumeration. See `3.23-3-next-directions.md`.

## Files

- `research-spike-1/prefix-utils.ts` — new (reusable prefix analysis utilities)
- `experiments/3.23-3-prefix-enumeration.ts` — new
- `experiments/3.23-3-solution-spot-check.ts` — new
- `experiments/output/3.23-3-prefix-enumeration-{slow,realistic}.txt` — output (gitignored)
- `experiments/output/3.23-3-solution-spot-check-{slow,realistic}.txt` — output (gitignored)
