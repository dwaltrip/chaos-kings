**NOTE:** Old naming convention — numbered for the target session, not the session that wrote it.

---

# HANDOFF doc from prev session

Research spike session on the perfect-start-solver. Orient from:

1. `packages/algos/src/perfect-start-solver/research-spike-1/docs/STATUS.md`
2. `packages/algos/src/perfect-start-solver/research-spike-1/docs/findings/3.21-l3-per-neighbor-pruning.md` (L3 result + pivot rationale)
3. `packages/algos/src/perfect-start-solver/research-spike-1/docs/sessions/3.21-1-LOG.md` (session log with reviewer feedback)

The neighbor partitioning line of work (L1/L2/L3) is complete. L1 is integrated into solver-v3 (50% candidate reduction on corner boards). L3 produced zero prunes — the bottleneck is combinatorial, not spatial. Time to pivot.

This session: **symmetry breaking + group ordering**, then **add harder test boards**.

Symmetry breaking:
- Fix burst-1 direction on symmetric boards. One `continue` statement, instant ~2x on symmetric degree-2 corners.
- Independent of everything else, stacks with all existing optimizations.

Group ordering:
- Solver currently tries longest-burst-1 groups first, exhausting them before shorter ones.
- If the solution uses a shorter burst-1, this wastes significant work.
- Reorder or interleave groups by feasibility heuristic. ~10 lines.

Harder test boards:
- Add boards with **asymmetric neighbor territories** (general at mouth of a narrow corridor with open space on the other side) — needed to exercise L3 and future spatial ideas
- Larger constrained boards that push solve time above 5s
- Review whether current test suite adequately stresses the solver

Follow-up if time permits:
- **Phase timing** — measure search vs path-gen breakdown. Validate assumption that search dominates.

Workflow notes:
- **Check in with me before starting implementation.** Share your plan and get alignment before editing the main solver.
- Save experiment output to `experiments/output/` from the start (these are gitignored, not committed)
- Write session LOG before updating STATUS
- Commit after completing a big block of work
- Update `docs/WORKFLOW-LOG.md` with any workflow observations
- At session wrap-up, write a clean copy-pasteable next-session prompt as the last message.
