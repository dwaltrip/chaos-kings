## Context / high-level From planning session 

So the full board structure body of work becomes three layers:

1. Board topology — cut vertices, corridors, regions (graph algorithms)
2. Directional structure — BFS sectors, branching, capacity (BFS from general)
3. Spatial path analysis — tile scarcity, affinity refinement, constrained-tile ordering (needs path data)

Each builds on the previous but is independently useful. A session could tackle one layer and stop, or flow into the next if time allows.

---

## Sketch of a cleaner breakdown for C6-C9 (in ROADMAP.md)

### Board Structure Layer 1: Graph Topology

**What it is:** Structural features of the board graph, independent of general position. Standard graph algorithms.

**Analyses:**
- **Articulation points / cut vertices:** Tiles whose removal disconnects the graph. One DFS pass (Tarjan's algorithm). Identifies chokepoints. These are the n=1 case of vertex cut sets.
- **n-vertex cut sets:** Generalizes articulation points — a set of n tiles whose removal disconnects the graph. Many board regions may not have single-tile chokepoints but do have 2- or 3-tile cut sets (isolated with regions with 2 chokepionts, narrow passages 2 tiles wide, etc). n=2 and n=3 are likely the most relevant cases. More expensive to compute than articulation points but could reveal structural constraints that single cut vertices miss.
- **Corridor detection:** Chains of tiles with exactly 2 walkable neighbors. These are forced-path segments — any burst traversing this region must follow the corridor.
- **Region decomposition:** What connected regions do the cut vertices (and n-vertex cut sets) create? A board with 3 cut vertices might decompose into 4 regions of known sizes. This gives a coarse structural map.
- **Bridge edges:** Edges whose removal disconnects the graph. Related to cut vertices but sometimes more informative (a tile might not be a cut vertex but an edge from it might be a bridge).

**Effort:** Low. All standard graph algorithms, O(V+E). Half a session including analysis across the board suite.

**What we'd learn:** How structurally constrained are realistic boards? Do 25x25 boards with random mountains have many cut vertices near the general, or are they mostly well-connected? This directly informs whether bottleneck-based reasoning (must-capture, dynamic reachability) is worth pursuing.

**Downstream consumers:** Must-capture analysis (layer 1 follow-up), dynamic reachability (parked B5 — cut vertex coverage as a cheap proxy), board difficulty characterization.

---

### Board Structure Layer 2: Directional Structure

**What it is:** How the board decomposes from the general's perspective. BFS-based, anchored to the general's position and neighbors.

**Analyses:**
- **BFS-based sector assignment:** For each walkable tile, which neighbor is it closest to (by BFS distance from each neighbor, excluding the general)? Assign tiles to their nearest neighbor. This gives a simple hard-boundary sector map.
- **Sector capacity & balance:** How many tiles per sector? Ratio of largest to smallest. Imbalanced sectors mean one direction has much more territory — relevant for burst-to-neighbor assignment.
- **BFS tree branching:** From each neighbor, where are the major branch points? What are the subtree sizes? This tells us where overlap bursts through the same neighbor must diverge (connects to the non-backtracking / tree structure insight).
- **Contested tiles:** Tiles equidistant from 2+ neighbors (ties in BFS assignment). How many? Where? These are the tiles that could be served by bursts from multiple directions.

**Effort:** Low to moderate. BFS infrastructure already exists from L3 (`NeighborInfo.blankMasks`, `board-bfs.ts`). Main work is computing the metrics and analyzing across the board suite.

**What we'd learn:** Are sectors clean (80/20 owned vs contested) or messy (50/50)? Does sector balance correlate with solve difficulty? Is the degree-2 decomposition into nearly-independent subproblems a general pattern or specific to toy boards?

**Downstream consumers:** B4-light/B4-full (per-sector feasibility), sector-aware search ordering, the "degree-2 independent subproblems" idea.

---

### Board Structure Layer 3: Spatial Path Analysis

**What it is:** How generated paths distribute across the board. Requires path data (from path gen). Bridges board structure and path structure.

**Analyses:**
- **Path inclusion counts per tile:** Across all paths (all lengths), how many include tile T? Histogram of inclusion counts. Identifies scarce tiles (low count) vs flexible tiles (high count).
- **Path inclusion per neighbor per tile:** Of the paths through neighbor N, how many include tile T? This refines the BFS-based sector assignment from Layer 2 — it's the "fuzzy affinity" version. A tile might be BFS-closest to A but reachable by more paths through B (due to terrain routing).
- **Scarcity distribution:** Are scarce tiles clustered in specific regions? Near corridors? Near the board edges? Correlation with Layer 1 topology (cut vertices, corridors).
- **Per-length breakdown:** How does inclusion count vary by path length? A tile might be in many length-12 paths but few length-4 paths, which matters for specific burst sizes.

**Effort:** Moderate. Path inclusion counts require iterating all generated paths (already available). Analysis and visualization across the board suite.

**What we'd learn:** How much scarcity variation exists on realistic boards? If most tiles have similar inclusion counts (high slack, open boards), scarcity-based ordering adds little. If there's a long tail of scarce tiles, constrained-tile-first ordering and forced-move propagation become promising.

**Downstream consumers:** Constrained-tile-first ordering (search ordering heuristic), forced-move propagation (D12), affinity refinement for sectors (connects back to Layer 2).
