{
  "name": "generals-v2-core",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "terrain-demo": "npx ts-node terrain-demo.ts",
    "test": "jest --silent --verbose=false --reporters=summary",
    "test:verbose": "jest --verbose",
    "test:watch": "jest --watch --silent --verbose=false --reporters=summary"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.0"
  }
}
